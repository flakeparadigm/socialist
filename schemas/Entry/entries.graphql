query entries($list: ID, $creator: ID, $complete: Boolean) {
  entries(list: $list, creator: $creator, complete: $complete) {
    id,
    owner { id },
    creator { id },
    title,
    url,
    complete
  }
}
